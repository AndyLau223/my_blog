{
  "name": "Node.js",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-22-bullseye",
  
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "streetsidesoftware.code-spell-checker"
      ]
    }
  },
  
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Hello Remote World",
      "onAutoForward": "notify"
    }
  },
  
  "postCreateCommand": "git config --global --unset http.proxy && git config --global --unset https.proxy && git config --global --add safe.directory /workspaces/my_blog && cd my-blog && npm install",
  "remoteUser": "node"
}